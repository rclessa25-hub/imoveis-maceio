name: Gerar Relatório

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Criar relatório
      run: |
        mkdir -p reports
        
        # Criar index.html simples
        cat > reports/index.html << 'HTML'
        <!DOCTYPE html>
        <html>
        <head>
            <title>Relatório - Imóveis Maceió</title>
            <style>
                body {
                    font-family: Arial, sans-serif;
                    background: #0d1117;
                    color: white;
                    padding: 20px;
                }
                h1 { color: #2ea44f; }
                .card {
                    background: #161b22;
                    border: 1px solid #30363d;
                    border-radius: 10px;
                    padding: 20px;
                    margin: 20px 0;
                }
            </style>
        </head>
        <body>
            <h1>📋 Relatório do Projeto</h1>
            <p><strong>Imóveis Maceió</strong></p>
            
            <div class="card">
                <h3>✅ Status: Ativo</h3>
                <p>Sistema de relatórios funcionando!</p>
            </div>
            
            <div class="card">
                <h3>🔗 Links</h3>
                <ul>
                    <li><a href="https://rclessa25-hub.github.io/imoveis-maceio/" target="_blank">Site Principal</a></li>
                    <li><a href="https://github.com/rclessa25-hub/imoveis-maceio" target="_blank">Repositório</a></li>
                </ul>
            </div>
            
            <p>Gerado em: $(date)</p>
        </body>
        </html>
        HTML
        
        echo "✅ Relatório criado em reports/"
    
    - name: Configurar Pages
      uses: actions/configure-pages@v3
    
    - name: Upload
      uses: actions/upload-pages-artifact@v1
      with:
        path: './reports'
    
    - name: Deploy
      id: deployment
      uses: actions/deploy-pages@v2
